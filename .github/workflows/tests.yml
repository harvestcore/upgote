name: Tests

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v2

    - name: Install deps
      run: make install

    - name: Run tests
      run: make test
